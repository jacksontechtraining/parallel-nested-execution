pipeline{
    agent any
    stages{
        stage('squential execution 1'){
            steps{
                echo "non parallel execution"
            }
        }
        stage('squential execution 2'){
            steps{
                echo "non parallel execution"
            }
        }

        // stage("Prallel Stage"){

        // }
    }
}